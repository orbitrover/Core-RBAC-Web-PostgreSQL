@{
    ViewData["Title"] = "Manage User Roles";
}

<h4>Manage User Roles</h4>
<p>
    Work in Progress till then you can visit on <a href="/Admin/Users" class="btn btn-outline-primary" >Users</a>
</p>

@* <div class="row mt-3">
    <div class="col-md-12">
        @if (ViewBag.FilterOption != null)
        {
            foreach (var Ctr in ViewBag.FilterOption)
            {
                <a id="FilterOptions_@Ctr" class="btn btn-outline-info btn-sm"
                   href="/Admin/UserList?filterOption=@Ctr" data-modal-filter-option="">@Ctr</a>
            }
        }
    </div>
</div>


<div class="row mt-4">
    <div class="col-md-6" id="replacetarget">
    </div>
    <div class="col-md-6" id="replacetarget2">
    </div>
</div>
<script>
    window.onload = function () {
        $('#replacetarget').load('/Admin/UserList?filterOption=All');
        $('#replacetarget2').load('/Admin/RoleGroupList?filterOption=All');
    };
    $(document).on("click", "a[data-modal-filter-option]", function (e) {
        $('#replacetarget').load(this.href);
        return false;
    });
</script> *@

